let fetch = require('node-fetch')

	conn.sendFile(m.chat, `https://api.lolhuman.xyz/api/random/elf?apikey=${global.lolkey}`, 'Kikuchanj.jpg',, m)

handler.command = /^(yaoi)$/i
handler.tags = ['nsfw']
handler.help = ['XD']

export default handler
